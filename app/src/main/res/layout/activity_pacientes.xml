<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_pacientes"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.evidencia.godhc.CitasMedicas.Pacientes">

    <ImageView
        android:layout_width="250dp"
        android:layout_height="150dp"
        app:srcCompat="@drawable/logo"
        android:layout_marginTop="25dp"
        android:id="@+id/logo"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:layout_marginEnd="58dp" />

    <Button
        android:text="Revisar expedientes"
        android:layout_height="wrap_content"
        android:layout_marginBottom="76dp"
        android:id="@+id/btn_expedientes"
        android:background="@drawable/agenda"
        android:textColor="#FAFAFA"
        android:layout_width="200dp"
        android:layout_alignParentBottom="true"
        android:layout_alignEnd="@+id/logo"
        android:layout_marginEnd="15dp" />

    <Button
        android:text="Crear expediente"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:id="@+id/btn_crear"
        android:background="@drawable/agenda"
        android:textColor="#FAFAFA"
        android:layout_marginBottom="53dp"
        android:layout_above="@+id/btn_expedientes"
        android:layout_centerHorizontal="true" />


</RelativeLayout>
